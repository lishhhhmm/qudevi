# qudevi

Lightweight SQL query dependency visualizer.

Quick start

Run with Docker (recommended):

```bash
docker compose up -d --build
```

Or locally:

```bash
npm install
npm run dev
```

Open http://localhost:3000
